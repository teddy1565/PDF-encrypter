{
  "name": "pdf-encrypt",
  "version": "1.0.0",
  "description": "",
  "main": "./pdf-encrypt/src/index.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-packager ./ main --platform=win32 --arch=x64 --electron-version=10.1.3 --asar=true --out=PDFencrypt --ignore=node_modules/electron-* --version-string.CompanyName=PDFencrypt --version-string.ProductName=PDFencrypt"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-qpdf": "^1.0.3",
    "pdf": "^0.1.0",
    "qpdf": "0.1.0-prealpha.1"
  },
  "devDependencies": {
    "electron-rebuild": "^2.3.2"
  }
}
